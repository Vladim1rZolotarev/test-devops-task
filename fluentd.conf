<source>
  @type forward
  port 24224
</source>

<match flask.logs>
  @type loki
  url "http://loki:3100/loki/api/v1/push"
  flush_interval 5s
</match>

<match nginx.logs>
  @type loki
  url "http://loki:3100/loki/api/v1/push"
  flush_interval 5s
</match>
